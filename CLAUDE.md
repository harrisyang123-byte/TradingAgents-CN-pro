# CLAUDE.md

> 本项目的 Agent 指南统一维护在 **[`AGENTS.md`](./AGENTS.md)**。
>
> 请直接阅读 `AGENTS.md`：链路、快捷指令、接力机制、`--industries` 开关、
> 17 个 v3 子 Agent 清单、设计铁律与硬前置都在那里。
>
> 本文件保留只为让 `claude -p` 启动时能找到入口，**不再单独维护内容**，
> 避免文档与代码两套互相飘移。
